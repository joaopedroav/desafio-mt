export class Departamento {
    id: string;
    codigo: string;
    descricao: string;

    constructor(id: string, codigo: string, descricao: string) {
        this.id = id;
        this.codigo = codigo;
        this.descricao = descricao;
    }

}